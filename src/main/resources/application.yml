server:
  address: localhost
  port: 8080

spring:
  jpa:
    generate-ddl: true
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5432/sweater
    username: u
    password: p
  freemarker:
    expose-request-attributes: true